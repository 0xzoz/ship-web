{
  "name": "ship-web",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "verify": "bash scripts/verify.sh | tee logs/verify.log"
  }
}
